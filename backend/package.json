{
  "name": "starter-kit-astro",
  "version": "1.0.0",
  "description": "Apostrophe starter kit project for Astro integration",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env app",
    "dev": "nodemon --exec \"node --env-file=.env\" app.js",
    "add-user": "node --env-file=.env app @apostrophecms/user:add",
    "build": "cross-env NODE_ENV=production node --env-file=.env app @apostrophecms/asset:build",
    "serve": "cross-env NODE_ENV=production node --env-file=.env app",
    "release": "npm install && npm run build && npm run migrate",
    "migrate": "cross-env NODE_ENV=production node --env-file=.env app @apostrophecms/migration:migrate"
  },
  "nodemonConfig": {
    "delay": 1000,
    "verbose": true,
    "watch": [
      "./app.js",
      "./modules/**/*",
      "./lib/**/*.js",
      "./views/**/*.html"
    ],
    "ignoreRoot": [
      ".git"
    ],
    "ignore": [
      "**/ui/apos/",
      "**/ui/src/",
      "**ui/public/",
      "locales/*.json",
      "public/uploads/",
      "public/apos-frontend/*.js",
      "data/"
    ],
    "ext": "json, js, html, scss, vue"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/apostrophecms/combined-astro-starter-kit"
  },
  "author": "Apostrophe Technologies, Inc.",
  "license": "MIT",
  "dependencies": {
    "@apostrophecms/blog": "^1.0.4",
    "@apostrophecms/vite": "^1.1.0",
    "apostrophe": "^4.23.0",
    "cross-env": "^10.1.0"
  },
  "devDependencies": {
    "eslint-config-apostrophe": "^6.0.1",
    "nodemon": "^3.0.1"
  }
}
