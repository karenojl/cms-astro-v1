import { Figure } from "@/components/react/Figure";
import type { ImageWidgetViewProps } from "@/types/widgets";

export function ImageWidgetView(props: ImageWidgetViewProps) {
  return <Figure {...props} />;
}
