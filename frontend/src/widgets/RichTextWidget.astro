---
import { RichTextWidgetView } from "@/widgets/react/RichTextWidgetView";
import type { RichTextWidgetData } from "@/types/widgets";

interface Props {
  widget?: RichTextWidgetData;
}

const { widget = {} } = Astro.props as Props;
---

<RichTextWidgetView content={widget.content} />
