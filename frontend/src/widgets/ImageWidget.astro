---
import { buildImageWidgetPresentation } from "@/lib/image-widget";
import { ImageWidgetView } from "@/widgets/react/ImageWidgetView";
import type { ImageWidgetData } from "@/types/widgets";

interface Props {
  widget?: ImageWidgetData;
}

const { widget = {} } = Astro.props as Props;
const presentation = buildImageWidgetPresentation(widget);
---

<ImageWidgetView {...presentation} />
